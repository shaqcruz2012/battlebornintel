{
  "name": "battlebornintel",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "set -e && cd api && npm ci --omit=dev && cd ../frontend && npm ci && VITE_API_URL= npm run build && rm -rf ../api/public && mkdir -p ../api/public && cp -r dist/* ../api/public/",
    "start": "cd api && node src/index.js"
  }
}
